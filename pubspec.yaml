name: my_flutter_app
description: A new Flutter project.

# The following defines the version and build number for your application.
version: 1.0.0+1

environment:
  sdk: '^3.1.0'

dependencies:
  flutter:
    sdk: flutter

  # Additional dependencies here
  http: ^1.1.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Additional dev dependencies here

flutter:
  uses-material-design: true

  # Additional Flutter assets and fonts here
